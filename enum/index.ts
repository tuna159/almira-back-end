export enum EIsIncognito {
  NOT_INCOGNITO = 0,
  INCOGNITO = 1,
}

export enum EActivityType {
  COMMENT = 0,
  LIKE = 1,
}
export enum EIsDelete {
  NOT_DELETE = 0,
  DELETED = 1,
}
export enum ECommonStatus {
  NO = 0,
  YES = 1,
}
